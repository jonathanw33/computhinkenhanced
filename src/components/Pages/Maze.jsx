import MazeGenerator from '../Labirin';  // Path is correct now as we're in Pages directory

const Maze = () => {
  return (
    <div>
      <MazeGenerator />
    </div>
  );
};

export default Maze;  // Add this export